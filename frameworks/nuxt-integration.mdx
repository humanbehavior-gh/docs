---
title: 'Nuxt.js'
description: 'Set up HumanBehavior SDK with Nuxt.js applications'
---

# Using HumanBehaviorTracker with Nuxt.js

## Quickstart

Human Behavior works seamlessly to record sessions on your Nuxt.js app.

<Expandable title="How to get your API Key">
<Info>
  <strong>How to find your Public API Key:</strong><br/>
  <b>Flow:</b> Project Home → Integrations → Human Behavior SDK → Kebab Menu → Configuration → Public API Key
</Info>

<img
  src="/images/APIKeyUpdated.png"
  alt="Where to find your Public API Key"
  style={{ borderRadius:"0.5rem",margin:"1rem 0",maxWidth:"100%" }}
/>

<p style={{ fontSize: '0.95em', color: '#888', marginTop: '-0.5rem', marginBottom: '1.5rem' }}><em>Example screenshot – your actual API key will be different.</em></p>
</Expandable>

### Option 1: Installation Wizard (Recommended)

The easiest way to get started is using our installation wizard:

```bash
npx humanbehavior-js YOUR_API_KEY
```

You'll see several installation options:

```bash
🤖 Choose installation mode:
1. Manual AI-Enhanced (recommended) - Choose your framework and use AI for optimization
2. Traditional - Standard framework detection and installation
3. Browser - Browser-based AI detection and code generation

Select mode (1-3, default: 1):
```

Select **Option 1: Manual AI-Enhanced** and then choose **Nuxt** from the framework list:

```bash
🎯 Choose your framework:
1. Vanilla JS (HTML)
2. React
3. Next.js
4. Vue
5. Nuxt <
6. Angular
7. Svelte
8. Remix
9. Node.js
10. Other (Auto-detect)

Select framework (1-10):
```

The AI wizard will then:
1. Analyze your Nuxt.js codebase for optimal integration
2. Detect your Nuxt configuration and dependencies
3. Install the humanbehavior-js package
4. Create plugins/humanbehavior.client.ts
5. Update nuxt.config.ts with runtime config
6. Configure environment variables
7. Provide Nuxt.js-specific optimizations and recommendations

**Version Compatibility**: The wizard works with most Nuxt versions and automatically detects:
- TypeScript support
- Modern Nuxt 3 patterns
- Package managers: npm, yarn, pnpm

### Option 2: Manual Setup

If you prefer manual setup or the wizard doesn't work for your project, follow the steps below.

## Step 1: Installation

```bash
npm install humanbehavior-js
# or
yarn add humanbehavior-js
```

## Step 2: Basic Setup

### Add the Tracker to Your App

Create a plugin in `plugins/humanbehavior.client.ts`:

```typescript
import { HumanBehaviorTracker } from 'humanbehavior-js';

export default defineNuxtPlugin(() => {
  const config = useRuntimeConfig();
  
  // Initialize the tracker once globally (client-side only)
  const apiKey = config.public.humanBehaviorApiKey;
  
  if (apiKey) {
    const tracker = HumanBehaviorTracker.init(apiKey);
    
    return {
      provide: {
        tracker
      }
    };
  }
  
  return {
    provide: {
      tracker: null
    }
  };
});
```

<Note>
  **That's it!** The SDK now automatically tracks user interactions, navigation, and console events. Everything past here is optional.
</Note>

## Optional: Advanced Features

### Track Custom Events

```vue
<template>
  <div>
    <h1>My Component</h1>
    <button @click="handleButtonClick">Click Me</button>
  </div>
</template>

<script setup>
import { onMounted } from 'vue';

const { $tracker } = useNuxtApp();

onMounted(() => {
  // Track component view
  $tracker.customEvent('component_viewed', {
    componentName: 'MyComponent'
  });
});

async function handleButtonClick() {
  await $tracker.customEvent('button_clicked', {
    buttonLocation: 'header'
  });
}
</script>
```

### User Authentication Example

```vue
<template>
  <div>
    <form @submit.prevent="handleLogin">
      <input v-model="email" type="email" placeholder="Email" />
      <input v-model="password" type="password" placeholder="Password" />
      <button type="submit">Login</button>
    </form>
  </div>
</template>

<script setup>
import { ref } from 'vue';

const email = ref('');
const password = ref('');
const { $tracker } = useNuxtApp();

async function handleLogin() {
  try {
    // Your login logic here
    const user = await login(email.value, password.value);
    
    if (user) {
      // Identify user with current endUserId
      await $tracker.identifyUser({
        userProperties: {
          email: user.email,
          name: user.name,
          userId: user.id,
          plan: user.plan
        }
      });
    }
  } catch (error) {
    console.error('Login failed:', error);
  }
}
</script>
```

## Environment Variables

```bash
# .env
NUXT_PUBLIC_HUMANBEHAVIOR_API_KEY=your-api-key-here
```

**Add to your `nuxt.config.ts`:**
```typescript
export default defineNuxtConfig({
  runtimeConfig: {
    public: {
      humanBehaviorApiKey: process.env.NUXT_PUBLIC_HUMANBEHAVIOR_API_KEY
    }
  }
});
```

```typescript
// In your plugin
const apiKey = config.public.humanBehaviorApiKey;
```

## Troubleshooting

- Check browser console for any error messages
- Verify your API key is correct
- Ensure the tracker is initialized before any user interaction 